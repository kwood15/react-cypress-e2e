import React from 'react'

const TodoForm = () => (
  <form>
    <input
      type='text'
      className="new-todo"
      placeholder="What needs to be done?"/>
  </form>
);

export default TodoForm;
